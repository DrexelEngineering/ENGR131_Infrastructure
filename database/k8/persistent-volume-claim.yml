apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc-josh # POSTGRES_PVC
  labels:
    type: local
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 25Gi # POSTGRES_PVC_SIZE
  storageClassName: rook-ceph-block #STORAGE_CLASS
